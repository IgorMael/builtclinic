<%= form_with local: true, method: :get do |f| %>
    <div class="row">
        <div class="col s10">
        <%= f.select "doctor",
            options_from_collection_for_select(Doctor.all.decorate, 'id', 'title_name',  @doctor&.id),
            include_blank: "Todos os médicos", class: 's8' %>
        </div>
        <div class="col s2">
            <%= f.submit 'Buscar', class: 'btn' %>
        </div>
    </div>
<% end %>